# StockManagerBackend
A multi-branch Django + PostgreSQL stock API enforcing strict role-based access, non-negative inventory, 6 movement types (sales, transfers with two-step confirmation, entries, adjustments, returns, donations), atomic transactions, row-level locking, and an immutable audit trail.
